# Seboruco | Historia, Cultura y Tradiciones

Página web lista para publicar en GitHub Pages.

## Archivos incluidos

- `index.html`
- `styles.css`
- `script.js`
- carpeta `assets/`

## Imagen principal

La web está configurada para usar esta ruta:

```css
url("assets/iglesia de seboruco.jpg")
```

Por eso, dentro de la carpeta `assets` debes subir la imagen con este nombre exacto:

```text
iglesia de seboruco.jpg
```

Si luego decides renombrarla como `iglesia-seboruco.jpg`, cambia la ruta en `styles.css` en dos lugares:

```css
url("assets/iglesia-seboruco.jpg")
```

## Cómo publicarlo en GitHub Pages

1. Crea un repositorio en GitHub.
2. Sube `index.html`, `styles.css`, `script.js` y la carpeta `assets`.
3. Dentro de `assets`, sube la imagen `iglesia de seboruco.jpg`.
4. Entra en `Settings` > `Pages`.
5. En `Build and deployment`, selecciona la rama `main` y la carpeta `/root`.
6. Guarda los cambios.
7. GitHub te dará el enlace público de la página.

## Recomendación

Para evitar problemas en sitios web, se recomienda usar nombres sin espacios, por ejemplo:

```text
iglesia-seboruco.jpg
```

Pero esta versión ya está adaptada al nombre que indicaste: `iglesia de seboruco.jpg`.
